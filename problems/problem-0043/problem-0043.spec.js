'use strict';
const problem43 = require('./problem-0043');

describe('substringDivisibility', () => {
  xit('contains sample pandigital', () => {
    expect(problem43.substringDivisibility()).toContain(1406357289);
  });
});
